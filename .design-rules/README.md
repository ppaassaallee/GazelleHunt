# Design rules

Copy of Apple Design Skill for RYVO UI reviews.

Install in a dedicated iteration:

```bash
curl -fsSL https://raw.githubusercontent.com/dickwu/apple-design-skill/main/SKILL.md -o .design-rules/SKILL.md
```

See `.cursor/rules/apple-design.mdc`.
